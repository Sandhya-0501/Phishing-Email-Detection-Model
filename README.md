The **Phishing Email Detection Model** is a machine learning application that identifies whether an email is phishing or safe based on its content.
It uses Scikit-learn to train a model on sample email data and analyze text features.
The system extracts keywords and patterns such as suspicious links or urgent messages to make predictions.
A simple web interface allows users to input email text and view real-time classification results.
